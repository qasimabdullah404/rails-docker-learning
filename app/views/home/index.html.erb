<%= @message %>

<% @posts.each do |post| %>
  <h3> <%= post.title %> by <%= post.author.name %> </h3>
  <p> <%= post.body %> </p>

  <br>

  <p> 
    <%= link_to "Like", increment_async_path(post_id: post.id), method: :post %>
  </p>

  <p>  Likes:
    <%= post.likes_count %> 
  </p>
<% end %>